# Remas UI (React)

Run locally:

```
cd react-ui
npm install
npm run dev
```

Place exported Blender GLB/GLTF models under `react-ui/public/models` or serve them from a URL and use the `Model URL` input in the `Psi` panel.
